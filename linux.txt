1.虚拟机克隆右键->管理->克隆2.快照相当于存档右键->快照3.迁移剪切就可以4.设置共享文件安装vmtools，右击虚拟机，选项中选共享文件夹在虚拟机中找到mnt文件夹，共享文件夹就在其中5，目录结构根目录都是规定好的，且某些特定文件放在特定根目录下例如root：该用户所下载的文件boot：系统启动所需文件dev:设备管理器/bin：常用指令/sbin：管理员用的系统管理程序/home：普通用户主目录/root:管理员/lib：动态链接共享库/lost+found：一般为空，存放非法关机时错误/etc:系统管理所需配置文件和子目录，mysql的my.conf/usr:应用程序/boot：系统启动所需文件/proc：内存映射				}/srv：服务					} 轻易不能动/sys：内核					}/tmp:临时文件/dev:硬件映射/media：识别外设/mnt：挂载外部文件/opt：安装文件存放目录/usr/local:安装文件安装目标目录/var：放日志/selinux：安全子系统，启用时才有6.远程登录使用xshell：linux下用ifconfig获取ipwin下看是否ping通，然后使用xshell进行控制台操作7.远程文件下载使用xftp：进行传输，也可把lunix文件传到win8：vi和vim命令行：vim hello.java      :用vim编写这个文件回车后按i进入插入模式      输入内容输入esc在输入：              进入命令行模式:wq保存并退出:q!强制退出一般模式下：yy复制当前行，p粘贴，dd删除当前行nyy,ndd对n行进行相应操作输入/进入命令行模式，/关键字：    查找这个词，输入n查下一个这个词:set nu显示当前行:set nonu不显示当前行G文件末尾       gg文件首       u撤销nG第n行9.开机重启，登录注销shutdown -h now立刻关机shutdown -h n     n分钟后关机shutdown -r now立刻重启halt 			关机reboot		重启sync			将内存数据同步到磁盘su - root            获得root用户权限logout 退回原用户，logout 退出登录10.用户管理useradd 用户名           ：在home目录添加用户useradd -d 指定目录 用户名passwd 用户名		：设置密码pwd                           :显示当前用户所在目录userdel 用户名		：删除用户，但保留主目录userdel -r 用户名	：不保留主目录whoami			:显示当前登录的用户信息who am i			:显示第一次登录的用户信息11.用户组clear或ctrl+L		：清空显示groupadd 组名		：创建组groupdel 组名groups 组名		：查看组内成员useradd -g 用户组 用户名id 用户名			：查找用户及所在组usermod -g 用户组 用户名		：将用户移到其他组/etc/passwd文件		用户配置/etc/shadow文件		口令配置/etc/group文件			组配置12.运行级别init 数字			：切换运行级别systemctl get-default	 	：查看当前级别systemctl set-default multi-user.target		:切换为级别3此时重启，设置默认级别就为313.如何找回root密码开机界面输入e找到Linux16所在行末尾，输入init=/bin/sh输入ctrl+x输入mount -o remount,rw		回车设置密码touch /.autorelabelexec /sbin/init14.帮助信息man 命令		：获取帮助信息help 命令		：获取shell内置命令的帮助信息隐藏文件以 . 开头选项可以组合使用 :    ls -al /root		已单列形式展示root目录下的所有文件15.目录指令在home目录下绝对：/home/tom/a.txt相对：tom/a.txtcd ~		回到家目录cd ..		回到上一级目录cd ../../root		退回两级去rootmkdir /home/dog		创建一级目录mkdir -p /home/animal/tiger	创建多级目录rmdir /home/dog		删空目录rm -rf /home/animal		强制删除touch 文件名			创建空文件cp 文件 目录			将文件拷贝到目录下cp -r 拷贝目录 目标目录	将目录拷贝到目录下\cp		强制覆盖不提示提示时点y/nmv 		同一目录下重命名，不同目录剪切mv cat.txt pigtxt			前文件改为后文件mv /home/pig.txt /root可以移动目录cat 文件				查看文件内容-n   			显示行号| more		空格翻页，回车显行less 文件名		查看文件pgup向上翻，pgdown或空格向下翻/子串		查找，输入n向下查，N向上查echo	内容			输出内容echo $HOSTNAME	输出主机名head 文件			查看前十行head -n 5 文件		查看前五行tail 文件			查看后5行tail -n 5 文件-f 实时追踪更新			ctrl+c退出监控命令 > 文件		输出存入文件>覆盖            >>追加cal 			显示当前日历ln -s /root /home/myroot		创建一个快捷方式history 		查看历史命令history 10		最近10个!5			执行第五个指令16.时间日期date			当前时间%Y       %m	%d		%H		%M		%s年月日时分秒date "+%Y-%m-%d %H:%M:%S"date -s "2020-11-03 20:02:34"			设置系统时间cal		日历cal 2020    该年日历17.查找find 搜索范围 选项-name 文件名-user 用户名-size 文件大小		+大于，-小于，无符号等于			单位kMG-name "*.txt"	 查找所有.txt文件find / -size +200Mlocate:必须先updatedb更新数据库locate 文件名which 指令		查指令在哪个目录cat a.txt | grep -n "hello"		过滤查找内容或grep -n "hello" a.txt-n 显示行号-i 忽略大小写grep -v		反向匹配18.压缩和解压gzip 文件		    压缩gunzip 文件.gz	    解压zip 	XXX.zip	    压缩-r	压缩目录unzip  XXX.zip	    解压-d     选择目标目录zip -r myhome.zip /home/unzip -d /opt/tmp /home/myhome.ziptar XXX.tar.gz 打包内容-c     	产生.tar文件-v	显示详细信息-f	指定文件名-z	打包同时压缩-x	解包.tar文件-C	指定解包目录tar -zcvf pc.tar.gz /home/poo.txt /home/car.txttar -zxvf /home/pc.tar.gz -C /opt/tmp19.组管理，权限管理ls -ahl	直观显示文件所有者chown 用户名 文件名			改变文件所有者-R:目录chgrp  组名  文件名			改变文件所在组-R:目录usermod -d 目录名 用户名		改变用户登录的初始目录ls -l中会出现显示信息第0位(d,-,l,c,b)l 	:链接，相当于快捷方式d	:目录，相当于文件夹-	:普通文件c	:字符设备文件，鼠标键盘b	:块设备，硬盘第1-3位	所有者拥有权限第4-6位	所属组拥有权限第7-9位	其他用户的权限rwx	读写执行用户名前数字:	子目录数chmod u=rwx,g=rx,o=x 文件或目录chomd 755 文件/目录chmod o+w 文件/目录chmod a-x 文件/目录			a是所有人20.crond 任务调度			周而复始-e 编辑任务-l 查询任务-r 删除当前用户所有任务任务调度文件	/etc/crontab*/1 * * * * ls-l /etc/ > /tmp/to.txt第一个*   一小时当中的第几分钟第二个*   一天当中的第几小时第三个*   一个月当中的第几天第四个*   一年当中的第几个月第五个*   一周当中的星期几，	不连续时间-	连续时间*/n	隔多久执行一次.sh	:脚本后缀，可写入多指令service crond restart     重启任务调度21.at定时任务要保证atd进程的启动		ps-ef | grep atdat 选项 时间ctrl+D	结束at输入-m 任务完成后，发送文件-I    atq的别名-d   atrm的别名-v    显示任务被执行的时间-c    打印任务内容-q <队列>  使用指定队列-f <文件>   指定文件读入-t <时间参数>  以时间参数形式提交运行的任务atrm 5		把5任务拿掉时间指定形式：04:00midnight noon teatime12pm04:00 2021-03-01now + 5 minutestoday tomorrow22.磁盘虚拟机设置添加磁盘lsblk     查看硬盘分区fdisk /dev/sdb  对/sdb进行分区m显示命令列表p显示磁盘分区		同-ln新增分区d删除分区然后格式化mkfs -t ext4 /dev/sdb1然后挂载mkdir /newdisk		创建挂载文件mount /dev/sdb1 /newdisk/   挂载unmount /dev/sdb1或unmount /newdisk/    卸载重启后挂载会失效可通过修改/etc/fstab实现永久挂载23.磁盘情况df -h       查看磁盘情况du  选项  目录			查询指定目录的磁盘占用情况-s	指定目录占用大小汇总-h     带计量单位-a     含文件--max-depth=1		查询深度-c     列出明细同时，列出汇总值du -ha --max-depth=1 /opt常用指令：ls -l /opt | grep "^-" | wc -l      以-开头的文件个数ls -l /opt | grep "^d" | wc -l      以-开头的目录个数ls -lR /opt | grep "^-" | wc -l	以-开头的所有文件个数（包括子文件）ls -lR /opt | grep "^d" | wc -l	以-开头的所有目录个数（包括子目录）tree /home/			以树状显示目录结构要先yum install tree下载包24.网络配置ping ip地址ifconfig 查看网络配置修改配置文件指定静态ip设置里改网关vim /etc/sysconfig/network-scripts/ifcfg-ens33加入：IPADDR=某ipGATEWAY=某ipDNS1=某ip将BOOTPROTO改成static25.设置主机名hostname   查看主机名vim /etc/hostname			去修改主机名如何通过主机名找到linux机？win：在C:\Windows\System32\drivers\etc\hosts文件中加上ip 主机名linux：/etc/hosts26.进程管理top: 打开前台进程ps： 查看正在执行的进程-a     所有进程-u     以用户格式显示信息-x     显示后台进程运行的参数-e     所有进程-f	全格式kill   选项   进程号	：消灭进程-9	：强迫停止killall  进程名称		：消灭进程pstree 选项		查看进程树-p：显示pid-u：显示进程所属用户27.服务管理service或systemctl在/etc/init.d中可查看可用service指令的服务（绿色）start，stop，restart，reload，statussetup     看到全部服务chkconfig --list          查看各个运行级别的服务情况chkconfig --level 5 服务名 on/off	开启或关闭自启动状态systemctl start/stop/restart/status 服务名			管理服务（临时有效）管理的服务在/usr/lib/systemd/system查看systemctl list-unit-files            查看服务开机启动状态systemctl enable 服务名		开启自启动（永久有效）systemctl disable 服务名		关闭自启动systemctl is-enabled 服务名	查询是否自启动firewall-cmd --permanent --add-port=端口号/协议			 打开端口firewall-cmd --permanent --remove-port=端口号/协议		 关闭端口firewall-cmd --reload		重新载入firewall-cmd --permanent --query-port=端口号/协议		查询是否开放28.动态监控top 选项-d 秒数			每隔几秒更新-i				不显示闲置或僵死进程-p				通过监控进程id来监控某个进程操作P			以cpu使用率排序M			以内存使用率排序N			以PID排序、q			退出u			查看用户进程k			终止某进程29.监控网络状态netstat 选项-an		按一定顺序排序-p		显示哪个进程在调用30.rpm下载包rpm -qa        查询已安装的rpm包rpm -q firefox     	查询是否安装rpm -qi firefox 		查询信息rpm -ql firefox 		查询包中的文件rpm -qf /etc/passwd		查询所属软件包rpm -e 包名		删除包rpm -e --nodeps 包名		强制删除rpm -ivh rpm包全路径名称		安装包yum list				查询包yum install firefox		下载包31.下载jdk创建文件夹/opt/jdk导入后解压把解压后的jdk放入/usr/local/java编辑/etc/profileexport JAVA_HOME=/usr/local/java/jdk···export PATH=$JAVA_HOME/bin:$PATH最后source /etc/profile刷新javac hello.java		编译java文件java hello.java			运行java文件32.下载tomcat/opt/tomcat				可执行文件为startup.sh开放8080端口浏览器中访问192.168.66.168:808033.idea下载解压至/opt/idea启动idea.sh,配置jdk编写程序并测试34.mysql下载35.shell编程开头加上#!/bin/bash执行：加x权限		./hello.sh可不加x权限      sh hello.sh36.shell变量系统变量：$HOME,$PWD,$SHELL,$USER等等显示shell中所有变量:     set自定义变量：定义变量：		变量名=值		（不能有空格）撤销变量：		unset 变量声明静态变量		readonly 变量		（不能unset）输出变量要加$A=`date`			反引号里为命令		也可以$()37.环境变量export 变量名=变量值source 配置文件echo $变量名在/etc/profile中定义多行注释			:<<!   内容	!$n	:$0 命令本身	$1 第一个参数	${10} 第10个参数$*	:所有参数			整体对待$@	:所有参数			区别对待$#	:参数的个数$$	:当前进程的进程号$!	:最后一个进程的进程号￥？ :最后一个命令的返回状态，0为成功文件	&		:以后台执行38.运算符$((运算式))或$[运算式]或expr m + nexpr运算符要有空格，将值赋给变量时用``expr \*,/,%39.条件判断[条件]		非空返回true&&      ||数字比较	-lt 小于 -le 小于等于 -eq 等于 -gt 大于 -ge 大于等于 -ne 不等于文件权限判断		-r 读权限 -w 写权限 -x执行权限文件类型判断		-f 存在并且常规 -e存在 -d目录1.判断ok是否等于okif [ "ok" = "ok" ]then		echo "equal"fi2.  23是否大于22if [ 23 -ge 22 ]then 		echo "大于"fi3.查看/root/aaa.txt是否存在if [ -f /root/aaa.txt ]then		echo "存在"fi1.输入大于等于60，及格		小于60，不及格if [ $1 -ge 60 ]then 	echo "及格"elif [ $1 -lt 60 ]then	echo "不及格"fi或者if [ $1 -ge 60 ]then 	echo "及格"else	echo "不及格"fi1.参数为1时，输出周一，参数为2时，输出周二，其他输出othercase $1 in"1" )		echo "周一";;"2" )	echo "周二";;* )	echo "other";;esac1.打印输入的参数（可看出$*和$@的区别）for i in "$*"					（不加引号会被分割）do	echo "num is $i"done输出一句话for i in "$@"					do	echo "num is $i"done输出n句话2.从1到100累加sum=0for (( i=1; i<=100; i++))do	sum=$[$sum+$i]doneecho "sum=$sum"1.计算从1加到nsum=0i=1while [ $i -lt $1 ]do	sum=$[$sum+$i]	i=$[$i+1]	doneecho "sum=$sum"40.read读取read 选项 参数-p 		指定读取时的提示符-t		指定读取时等待的秒数1.输入num1read -p "请输入num1:" num1echo "num1=$num1"2.输入num2，等待5秒read -t 5 -p "请输入num2:" num2echo "num2=$num2"41.函数basename	返回路径最后/的后面的内容basename /home/ifno.txt			输出ifno.txtbasename /home/ifno.txt   .txt		输出ifnodirname		返回路径最后/的前面的内容dirname /home/ifno.txt				输出/home自定义函数：1.计算两个参数的和function getsum(){	SUM=$[$n1+$n2]	echo ":$SUM"}read -p "输入n1：" n1read -p "输入n2: " n2getsum 需求分析：1.	2:30备份数据库sql_tutorial到/data/db2.  	备份开始和结束给出相应的提示信息3.	备份后的文件以备份时间为文件名，并打包为.tar.gz4.     备份同时检查10天前的文件，有就删除#备份目录backup=/data/db#时间datetime=` date "+%Y-%m-%d_%H%M%S" `echo $datetime#数据库地址host=localhost#用户名db_user=root#密码db_pw=_QazHimHxm24680#备份数据库名database=sql_tutorial#创建备份目录，如果不存在，就创建[ ! -d "${backup}/${datetime}" ] && mkdir -p "${backup}/${datetime}"#备份数据库mysqldump -u${db_user} -p${db_pw} --host=${host} -q -R --databases ${database} | gzip > ${backup}/${datetime}/$datetime.sql.gz#将文件打包cd ${backup}tar -zcvf $datetime.tar.gz ${datetime}#删除原文件rm -rf ${backup}/${datetime}#删除10天前的信息find ${backup} -atime +10 -name "*.tar.gz" -exec rm -rf {} \;echo "备份$database成功"crontab -e30 2 * * * /home/mysql_db.sh42.日志在/var/log/下/var/log/boot.log			系统启动/var/log/cron				定时任务/var/log/lastlog			最后一次登录，要用lastlog查看/var/log/mailog			邮件信息/var/log//message			重要信息/var/log//secure			验证授权账号密码/var/run/utmp				记录当前登录账号的信息,使用w，who，users命令查看rsyslog日志服务/etc/rsyslog.conf			日志配置文件自定义日志在/etc/rsyslog.conf日志轮替/etc/logrotate.conf全局配置weekly		每周进行一次轮替rotate		只保存4份create		轮替后创建新空文件dateext		使用日期作为后缀/etc/logrotate.d子配置daily								日志的轮替周期是每天weekly							日志的轮替周期是每周monthly							日志的轮替周期是每月rotate 数字						保留的日志文件的个数compress							日志轮替时，旧的日志进行压缩create mode owner group			建立新日志，同时指定新日志的权限与所有者和所属组。mail address						当日志轮替时，输出内容通过邮件发送到指定的邮件地址。missingok						如果日志不存在，则忽略该日志的警告信息notifempty						如果日志为空文件，则不进行日志轮替minsize 大小						日志轮替的最小值。也就是日志一定要达到这个最小值才会轮替，否则就算时间达到也不轮替size 大小							日志只有大于指定大小才进行日志轮替，而不是按照时间轮替。dateext							使用日期作为日志轮替文件的后缀。sharedscripts						在此关键字之后的脚本只执行一次。prerotate/endscript					在日志轮替之前执行脚本命令。postrotate/endscript				在日志轮替之后执行脚本命令journalctl			查看全部内存日志（还未写入硬盘）journalctl	 -n 3		查看最新三条journalctl	 --since 19:00 --until 19:10:10	查看起始时间到终止时间的日志journalctl -p err	报错日志journalctl -o verbose		详细内容journalctl _PID=1245 _COMM=sshd			查看包含这些参数的日志43.创建自己的linux对原系统参加一块盘，存储为单个文件fdisk /dev/sdb			进行分区mkfs.ext4 /dev/sdb1	格式化mkfs.ext4 /dev/sdb2挂载mkdir -p /mnt/boot /mnt/sysrootmount /dev/sdb1 /mnt/bootmount /dev/sdb2 /mnt/sysroot安装grub并拷贝至目标磁盘grub2-install --root-directory=/mnt /dev/sdb     hexdump -C -n 512 /dev/sdb		判断是否下载成功\cp -rf /boot/* /mnt/boot/vim /mnt/boot/grub2/grub.cfg		改一些磁盘分区 selinux=0 init=/bin/bash创建目标主机根目录系统mkdir -pv /mnt/sysroot/{etc/rc.d,usr,var,proc,sys,dev,lib,lib64,bin,sbin,boot,srv,mnt,media,home,root}cp /lib64/*.* /mnt/sysroot/lib64/cp /bin/bash /mnt/sysroot/bin/创建新虚拟机，并指向定制好的磁盘44.内核升级wget https://cdn.kernel.org/pub/linux/kernel/v6.x/linux-6.18.9.tar.gzyum info kernel -q			显示可升级的内核uname -a					查看当前内核版本yum update kernel			升级内核yum list kernel -q			查看以安装的内核45.备份dump和restore指令			（可能没有）yum -y install dump			yum -y install restoredump [-cu] [-012```9] [-f<备份后文件名>] [-T<日期>] [目录或文件系统]-c	创建新文件-012···9		级别，0为最完整-f	指定文件名-j	将文件压缩-T	指定备份日期-u	在/etc/dumpdares记录备份文件信息-t 	指定文件名-W 	显示需要备份的文件，并显示最后一次备份的信息-w	显示需要备份的文件dump -0uj -f /opt/boot.bak0.bz2 /bootdump -1uj -f /opt/boot.bak1.bz2 /boot			只备份新增加的cat /etc/dumpdatees			查看备份时间文件增量备份只支持分区，文件或目录不行restore 模式选项 选项四种模式：只能选一种-C		对比模式-i		交互模式，进行还原时，将询问用户-r		还原模式-t		查看模式选项：-f	<备份文件>		从指定文件中读取备份数据，进行还原操作(在进行操作的目录下）46.webmin下载和yum三个依赖重置webmin的root密码/usr/libexec/webmin/changepass.pl /etc/webmin root Qaz24680修改端口	（默认10000）vim /etc/webmin/miniserv.conf改prot和listen为8468重启webmin/etc/webmin/restart	重启	/etc/webmin/start		启动/ect/webmin/stop		停止开放8468端口firewall-cmd --zone=public --add-port=8468/tcp --permanent			开放端口firewall-cmd --reload								更新防火墙设置firewall-cmd --zone=public --list-ports				查看已开放端口47.bt宝塔wget -O install.sh http://download.bt.cn/install/install_6.0.sh && sh install.sh安装后会给出用户名和密码外网ipv4面板地址: https://112.98.235.220:15419/2e5c4438内网面板地址:     https://192.168.66.168:15419/2e5c4438username: uduxsw3tpassword: 797c827a48.题1.http://192.168.200.10/index1.htmlhttp://192.168.200.10/index2.htmlhttp://192.168.200.20/index1.htmlhttp://192.168.200.30/index1.htmlhttp://192.168.200.40/index1.htmlhttp://192.168.200.30/order.htmlhttp://192.168.200.10/order.html截取ip地址，按出现次数从大到小排序存入一个文件t.txtcat t.txt | cut -d '/' -f 3 | sort | uniq -c | sort -nr主机中的ip：netstat -an | grep ESTABLISHED | awk -F " " '{print $5}' | cut  -d ":" -f 1 | sort | uniq -c | sort -r 2.找出access.log文件中数量在前两位的ipcat access.log | awk -F " " '{print $1}' | sort | uniq -c | sort -nr | head -23.监听192.168.200.1:22的数据并记录在a。tcpdump -i ens33 host 192.168.200.1 and port 22 >> a.log4.chattr +i /etc/passwd		锁文件5.io读写iotop6。磁盘存储df -lh1.shell脚本如何检查文件存在if [ -f 文件名 ] then echo "存在" else echo "不存在" fi2.shell脚本对t3.txt的数字进行排序，输出总和sort -nr t3.txt | awk '{sum+=$0; print $0 } END {print "和="sum}'3.在home目录下找到包含cat数据的文件名grep -r "cat" /home | cut -d ":" -f 14.统计home目录下所有文件个数和文件总行数find /home -name "*.*" | wc -lfind /home -name "*.*" | xargs wc -laaaaaaa